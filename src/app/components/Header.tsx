import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="w-full h-12 opacity-20 md:fixed bg-white">
      {/* Header content */}
    </header>
  );
};

export default Header;